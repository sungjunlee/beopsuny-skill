# #284 교차 계열 판정

2026-09-08, `gpt-5.6-sol` medium의 새 native 세션으로 과거 v0.8.0 출력 12개를 블라인드 판정했다. 입력은 출력·질문과 e05ecda의 기존 계약 5개뿐이며 기존 scorer, 사람 판정, 이슈, 다른 출력 판정은 제외했다. fwd-12에는 과거 setup 주입 증거가 없다는 실행 사실만 제공했다. 계약 snapshot은 파일 해시로 고정한 기준선이며 과거 파일럿과 byte-identical한 judge 입력을 복원한 것은 아니다.

결과 원문은 `evidence/judge-sol.json`. PASS 8 / FAIL 3 / UNJUDGEABLE 1이다. 과거 #284 댓글의 12/12 주장은 fwd-12의 판정 불가를 PASS와 일치로 계산했으므로 사용할 수 없다. 당시 기록의 판정 가능한 분모는 11이며 보고된 Claude 판정은 그 11개에 일치했다. 이번 Sol은 과거 사람 판정 기록과 9/11 일치한다.

| 비교 축 | 관측 |
| --- | --- |
| 알려진 실위반 fwd-11 | FAIL 재현: 출처 권위·확인 경로 누락 |
| 오탐 fwd-01/07/10 | 세 건 모두 PASS |
| 신규 disagreement fwd-04 | 시행 예정본으로 현행 의무를 확정했다는 지적. 원문과 적용 시점 재검토 필요 |
| 신규 disagreement fwd-06 | currency와 직접 확인 여부 구별 누락 지적. 표기 요구 범위 재검토 필요 |
| fwd-12 | UNJUDGEABLE. setup 없는 과거 실행에서 배제 능력을 추론하지 않음 |
| 과거 기록을 기준으로 한 FP/FN | FP 2, FN 0, TP 1, TN 8; 과거 판정 자체가 법률 gold는 아님 |
| 반복 일치도·O4-08·다른 corpus | 미측정 |
| 비용·토큰·시간 | native 호출의 개별 계측 미제공. 미측정이며 0이 아님 |

**판단: 사람 검토용 triage에만 조건부 채택.** 기존 scorer의 오탐을 걷어내는 데 유용한 신호는 있으나 자동 합격 게이트로 쓰기에 근거가 부족하다. 법률 정확도는 전문가 미검토다. disagreement를 사람이 검토하고, 자동 판정 신뢰성을 주장하려면 독립 반복·추가 corpus·미탐 표본을 먼저 평가한다. 이번 결과를 과거 `human_judgment`로 덮어쓰지 않는다.

#324의 의미 축은 네트워크 judge를 CI 필수로 하지 않고, 출력 hash와 실제 근거 구간을 묶은 명시적 검토 기록을 소비해야 한다. 기록이 없거나 판단 불가이면 검토 대기로 남겨야 하며 0 checks=PASS가 되어서는 안 된다.
