---
generated: 2026-07-06
repo: sungjunlee/beopsuny-skill
trigger: "backlog-doctor reassess_signal — doctor clean; 6 sprints closed since last reassess (threshold 3); latest_report null (첫 reassess)"
mode: report-only
---

# Reassess Report — 2026-07-06

첫 spec-charter reassess. 편집 없음 — 수락된 항목은 `spec-charter amend`, `spec-system-map amend`, `spec-grill <capability>`, 또는 별도 승인된 Learning Action으로만 실행한다.

### Evidence

- `backlog-doctor --json`: 전 체크 pass (active-sprint 0은 informational). `reassess_signal` fired — 완료 스프린트 6개 / 임계 3, 과거 reassess 리포트 없음.
- `capabilities-doctor --json`: 3 capabilities / 134줄, 예산 내 (hard 500줄·15개). inline Learnings는 freshness-governance 4건 (max 7).
- `component-lint --json`: 스프린트 6개 중 5개 라우팅, 1개 미라우팅 — `2026-07-report-deliverable.md`(component `""`, capabilities.md 확정 전 종결된 legacy). issue 없음.
- `spec/charter.md`: revision 1 (2026-06-20) — 이후 6개 스프린트가 종결되는 동안 무수정. O1/O2 `validated`(src: execution), O3/O4 `active`(src: inferred). Decisions 표 비어 있음.
- `spec/capabilities.md`: 세 capability 모두 Out-of-scope에서 `contract-review`를 소유자로 지목하지만 해당 블록은 없음 (system-map 후보로만 존재).
- `spec/system-map.md` Candidate Capability Boundaries: 후보 6개 중 3개(source-citation, freshness-governance, output-role-destination)는 grill 완료, `router-loading`·`profile-practice-memory`·`contract-review` 미착수.
- Open backlog: #183(SKILL.md 2차 프루닝 실익 평가)은 router spine 영역 — 선언된 3개 capability 어디에도 라우팅되지 않는다. 에픽 #95/#97은 각각 profile-practice-memory·contract-review 후보 영역.

### No Change

- Charter Tier 1 (Problem/Approach/Non-Goals): 6개 스프린트 결과와 모순 없음. 프루닝(라우터 spine 유지), 리포트 레이어(단일 스킬 유지, 렌더 레이어를 기존 capability에 흡수), asset-hygiene(stale YAML 비권위 강화) 모두 Approach와 Non-Goals를 강화하는 방향으로 종결됨.
- `spec/system-map.md` Runtime Boundaries·Core Flows·Project-Wide Invariants: asset-hygiene 이후에도 정확 — clause_references는 여전히 "candidate data, not current-law authority"이고 retire(#207/#211)가 이를 더 강하게 만듦.
- capabilities.md 분량: 압박 없음. 분할·아카이브 불요.
- freshness-governance Learnings 4건: 전부 2026-07-05, 고신호 — inline 유지 (아래 승격 후보 1건 제외하면 조치 불요).

### System Map Candidates

- Candidate Capability Boundaries 절 — evidence: 후보 6개 중 3개가 이미 `spec/capabilities.md`에 landed됐는데 여전히 후보로 나열됨; suspected change: landed 3개를 capabilities.md 포인터로 강등하고 잔여 3개만 후보로 유지; next: `spec-system-map amend`. (경미 — 단독 실행보다 다음 amend에 동승 권장)

### Grill Candidates

- `router-loading` — evidence: 다음 스프린트 후보 #183(SKILL.md 다이어트)이 선언된 capability 어디에도 라우팅되지 않음 + system-map 후보에 근거 명시(SKILL.md intent router, `tests/scenarios/16_router_regression.yaml`, `check_router_*`) + 프루닝 스프린트에서 확정된 규칙(과잉 라우팅 판단은 Right-sizing 단일 기준, 품질 레이어 2겹)이 계약 문장 후보로 이미 존재; suspected change: 신규 capability 블록; next: `spec-grill` — SKILL.md 다이어트 스프린트 착수 전 권장.
- `contract-review` — evidence: 세 capability의 Out-of-scope가 소유자로 지목하는 블록이 부재, 에픽 #97/#110이 이 영역을 향함; suspected change: 신규 capability 블록(또는 Out-of-scope 문구를 후보 표기로 완화); next: `spec-grill` — 관련 스프린트 착수 전이면 충분, 지금은 급하지 않음.

### Amend Candidates

- O4 status (`active`, src: inferred) — evidence: smoke-findings·verification-hardening·output-modes 3개 스프린트가 objectives에 O4를 선언하고 종결(#196 DATA_ROOT semantics, #195 리포트 공식 링크, provenance 계열 작업 머지); suspected change: predicate("사용자가 Full/Lite 모드와 provenance 구분을 식별 가능") 충족 증거를 인용해 `validated` 승급하거나, 잔여 gap을 명시하고 `active` 유지; next: `spec-charter amend` — 증거 인용 없는 승급 금지.
- Decisions 표 공백 — evidence: 6개 스프린트에서 나온 cross-cutting 방향 결정(품질 레이어 2겹 기준, 죽은 자산 retire-first, freshness 검증 opt-out 집행)이 `_context.md`와 capability-level Decisions에만 존재; suspected change: 복수 capability에 걸치는 결정의 charter Decisions(Tier 3, append-only) 승격; next: `spec-charter amend`.

### Learning Actions

- Keep inline: freshness-governance Learnings 4건(2026-07-05) — 최신·고신호, startup context로 유효.
- Promote: "opt-in 검증은 사각지대를 낳는다 — 자산 전수 규칙은 opt-out(명시 allowlist)로 집행" [PR #209] — 테스트가 집행하는 durable rule이 됨; freshness-governance `### Decisions` 행 승격 후보; next: 별도 user-approved Learning Action.
- Archive: 없음 (전부 최근).

### Missing Evidence

- 과거 reassess 리포트 없음(첫 실행) — 추세 비교 불가. 이 리포트가 기준선.
- `capabilities-doctor`/`component-lint`는 structural-only(`coverage: not_assessed`) — 커버리지 판단은 파일 정독으로 대체함.
- O4 실사용 증거(smoke 로그의 Full/Lite·provenance 표시)는 이번 리포트에서 직접 재검증하지 않음 — amend 시 인용 필요.

### Recommended Next Step

- SKILL.md 다이어트 스프린트 착수 전에 spec-grill로 `router-loading` 후보 경계를 리뷰한다 (대화형·human-gated — 확정되면 스프린트 `component:`가 채워진다).
