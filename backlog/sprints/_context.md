# Project Context

dev-backlog 세션 시작 시 읽는 프로젝트 수준 컨텍스트. 스프린트를 넘어 재발견 비용이 큰 것만 남긴다.

- **profile/matter workspace 계약** (2026-07-profile-workspace, 에픽 #95 종결): practice profile(#104)과 matter workspace(#105) 모두 `references/memory-structure.md`가 단일 계약이고 capability `profile-practice-memory`가 앵커. matter=project(기존 `projects/{slug}/` 유지, 파괴적 rename 없음). workspace 파일 역할 4자 분리(source_log/reviews/outputs/history, owns·does-not-own 표), source-log(조회 provenance)≠verification-log(1차 확인), cross-matter gate = 기본 off·지명 예외·heightened 제외(HC2). 관련 정적 검사는 `check_memory_profile_workflow`에 집중 — 확장 시 삭제 없이 전문 assert 추가.
- **workflow map landed** (2026-07-workflow-suite, #109): `references/workflow-map.md`가 7개 workflow(commercial/privacy/labor/regulatory/litigation/startup/cross-border)를 기존 의도·reference로 매핑하는 virtual 인덱스. 새 라우터 의도 0(소비만), always-on gate 불변, split 경계는 DESIGN §6 정렬. 구조 검증 `check_workflow_map_structure`(참조 파일 실재+주 의도 실재 의도명+SKILL 의도 집합 동등성). litigation/cross-border 행에 #110/#112 자리표시만 있음.
- **contract-review capability landed** (2026-07-07 grill): spec-grill로 마지막 잔여 후보 결정화 → system-map Candidate Boundaries 잔여 0. HC1=counter-drafting bright line(완성 redline 금지), HC2=mode/playbook을 검증 약화 채널로 못 씀. 계약 검토 기존 동작을 명문화한 것이라 구현 스프린트 불요.
- **litigation court-style template landed** (2026-07-litigation-template, #110): research-workflow.md에 요건/인정/미확인/증거/잠정결론 5분리 판단 구조(Legal Verification Core 연결), output-formats.md에 판례 distinguishing(유사·차이·적용한계), workflow-map litigation 행 갱신. 형량·승패 예측 금지 유지. component source-citation.
- **litigation/enforcement workflow landed** (2026-07-08, #110·#111): research-workflow.md court-style 판단 구조 + output-formats distinguishing(#110, source-citation), enforcement-response.md 4 gate+7 시나리오+안전경계 3문구(#111, output-role-destination). 소송/처분 결과 예측 금지는 charter Non-Goal(revision 3). enforcement는 기존 high_risk_situations·role/destination gate 소비.
- **capability 지형 재검토 완료** (spec-grill map, 2026-07-08): litigation·enforcement 둘 다 **별도 capability 보류** — litigation은 distinguishing이 source-citation 소속·element-fact는 research-workflow 기능(표면 1섹션), enforcement는 전용 파일이나 단일 workflow 표면. enforcement의 homeless anti-obstruction bright line(증거 인멸·수사 방해 조력 금지)은 project-wide라 **charter Non-Goal(revision 4)로 승격** — capability 신설 대신. 표면 성장 시 재검토.
- **에픽 #97 종결** (2026-07-cross-border-overlay, #112 = 마지막 하위 이슈): cross-border는 `international_guide.md` `## 0. 우선 외국법 보조축 overlay`(9개 우선순위 영역→한국법 anchor+기존 의도)로 진입, 해외법은 **보조축**(결론 근거는 한국법 공식 원문, 후보 쟁점 인덱스+현지 전문가 확인). source-citation 계약 확장 = `source-grading.md` `### 4. 외국법 보조축 source rule`(해외 source에도 grading+verification tag, **jurisdiction/currency/source caveat DEFAULT 필수**, 해외 source는 한국법 결론 근거 불가). 짝 check `check_cross_border_overlay_roadmap`(전문 assert+`assert_not_contains` 자리표시). 새 라우터 의도·gate·capability 없음(6개 유지). 에픽 #97 = #109 map/#110 litigation/#111 enforcement/#112 cross-border 전부 merged. 후속 #220(AI Act 행 AI 기본법 anchor).
- **머지 감사 트레일 변형**: `after-internal-review` publish 정책은 리뷰가 publish **전** head에서 일어나 PR 코멘트가 없다 → relay-merge gate-check가 exit 1. 정당한 경로 = post-publication relay-review round를 `next_action: ready_to_merge` verdict로 재적용(review_pending→ready_to_merge)하면 finalize-run 가능. gate-check `--skip <reason>`는 감사 코멘트만 남기고 상태는 안 바꿈. (review_pending→ready_to_merge는 recover-state whitelist에 없음.)
- **테스트↔문서 강결합**: `tests/validate_skill_contracts.py`가 SKILL.md·references의 정확한 한국어 문구 수십 개에 `assert_contains`로 결합돼 있다. 문서 문구를 고치면 반드시 해당 check 함수를 먼저 grep해서 짝 수정할 것. 검증 게이트: `PYTHONPATH=.test-deps python3 tests/validate_skill_contracts.py`(O1) + `tests/evaluate_scenario_outputs.py`(O2).
- **backlog/tasks/는 gitignore** (PR #179 리뷰 결정): GitHub 이슈 미러는 sync-pull로 재생성 가능한 파생물이라 추적하지 않는다. 스프린트 파일과 완료 태스크(`backlog/completed/`)만 추적. dev-backlog 생성기가 `milestone: ` 후행 공백을 만들므로 completed/로 옮길 때 strip.
- **리포트 레이어 계약** (2026-07 스프린트, 에픽 #184): HTML 리포트는 `references/report-deliverable.md`가 단일 계약 — destination 계약 소비(R1), self-contained 규격+이스케이프(R2), 능력 기반 채널(R3), Artifact 공유 가정 gate+legal_effect_triggers 승급(R4), 새 의도 없음(R5). 템플릿은 `assets/templates/`. **템플릿 주석에 금지 패턴 리터럴을 넣지 말 것** — 생성 리포트에 잔류해 guardrail에 걸린다(static check가 부재를 검증). O2 기대 수치는 unsafe fixture 12개.
- **미러 currency 함정** (#180, PR #193): legalize-kr 미러는 최신 **공포본**을 담아 시행 전 개정 텍스트일 수 있다 (frontmatter `시행일자`가 미래인지 확인 — 의료법 제34조 사례). 미러 기반 revalidation·인용 시 공포일≠시행일 원칙을 미러 자체에도 적용할 것. 계약화는 이슈 #194.
- **relay 운용 노트**: 리뷰가 PR 본문 evidence를 요구하면 same-head 복구는 `recover-state.js --allow-same-head --require-pr-body-change`. 오케스트레이터 수정 커밋 후에는 `rebrand-evidence.js`로 evidence를 새 head에 재기록(게이트 재실행 근거 명시). **codex executor의 PR 본문은 최종 메시지 잘림으로 evidence가 truncate됨(2026-07-05 스프린트에서 4연속)** — dispatch 후 PR 본문(게이트 전체 문자열·mutation 기록·rubric 요구 표)은 오케스트레이터가 직접 작성/보강하는 것이 기본. **리뷰 changes_requested 후 redispatch는 fresh `dispatch.js -b <branch>`가 아니라 `dispatch.js . --run-id <run-id> --prompt-file <redispatch>`로 기존 run을 resume해야 한다(2026-07-07 #109)** — fresh dispatch는 in-flight 가드에 걸려 기존 run 정보만 출력하고 no-op(브랜치 커밋 안 됨). review-runner가 `review-round-N-redispatch.md`를 생성하니 거기에 오케스트레이터 보강 지시를 덧붙여 resume.
- **freshness 자산 규칙** (2026-07-asset-hygiene): assets/ 하위 YAML은 maintenance 의무(opt-out, 테스트 allowlist는 freshness-governance.md 기준의 집행자). stale 자산 retire의 우선 경로는 값 재검증이 아니라 단정 표현 제거(live-check-hint 전환). retire 시 non-retire revalidation fixture는 테스트 계약상 삭제 필연(git 이력 보존). last_verified는 git 이력 대조로 정직 기록 — 날짜 세탁 금지.
- **이동 문자열 보호 assert 규칙** (2026-07-skill-diet, PR #213 리뷰 R1): 계약 문자열을 다른 파일로 옮기며 assert를 따라 옮길 때, 라벨/부분 문자열이 **대상 파일에 원래 존재하면** 그 assert는 이동분 삭제를 못 잡는다 — 먼저 `git show main:<대상>` grep으로 기존 존재를 확인하고, 겹치면 bullet/문장 전문을 assert할 것. 검증은 삭제 mutation FAIL 재현으로.
- **프루닝 방향** (2026-07 스프린트, 에픽 #174): 아키텍처(단일 스킬 + 의도 라우터 + progressive disclosure)는 유지, 품질 레이어는 2겹(의도 라우터 gate 표 + citation-verification-contract)이 기준. 과잉 라우팅 판단은 SKILL.md 라우팅 원칙 1(Right-sizing)이 단일 기준. LVC 적용 강도는 light/full 2단 트리거. 죽은 자산은 retire-first(등록 아닌 삭제).
